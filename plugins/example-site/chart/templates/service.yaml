
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-example-site
spec:
  selector:
    app: {{ .Release.Name }}-example-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
